import { ProjectStatePipePipe } from './project-state-pipe.pipe';

describe('ProjectStatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectStatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
